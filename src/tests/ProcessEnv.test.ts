// const mockPENV: any = {};
// jest.mock('../processEnvEnums', () => mockPENV)
// import ProcessEnv from '../ProcessEnv'

describe('testing the penv list function', () => {
    beforeEach(() => {
        // jest.resetModules();
    });

    test('should list all keys and descriptions', () => {
        // mockPENV.X = { description: 'x' }
        // console.log = jest.fn(); // eslint-disable-line no-console
        // const res = new ProcessEnv().list();
        // expect(res).toEqual('X: x\n')
    })
});
